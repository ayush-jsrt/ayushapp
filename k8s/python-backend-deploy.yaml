apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-backend-deployment
  labels:
    app: python-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-backend
  template:
    metadata:
      labels:
        app: python-backend
    spec:
      containers:
        - name: python-backend
          image: ayushjsrtia/ayushapp-backend
          ports:
            - containerPort: 5000